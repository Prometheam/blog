---
layout: post_layout
title: "explicit关键字"
date: 2025-10-11 15:10:08 +0800
categories: [C++语言]
location: 西安
excerpt_separator: "```"
---
是一个关键字，主要用于 C++ 语言中，用于**修饰类的构造函数**。它的作用是防止编译器进行隐式的类型转换，从而避免潜在的错误或歧义。

### **作用：**
- **禁止隐式转换**：
当构造函数被声明为  时，编译器不会自动调用该构造函数进行隐式类型转换，必须显式地调用构造函数。
- **避免意外的行为**：
某些情况下，隐式转换可能导致逻辑错误或难以发现的 bug。 强制要求程序员明确表达意图。

### **示例：**

</pre>
### **适用场景：**
- **单参数构造函数**：
如果构造函数只有一个参数（或多个参数但有默认值），通常建议用  避免隐式转换。
- **避免歧义**：
例如  的构造函数  防止误写为 。
