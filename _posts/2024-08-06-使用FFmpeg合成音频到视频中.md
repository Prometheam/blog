---
layout: post_layout
title: "2024-08-06-使用FFmpeg合成音频到视频中"
date: 2024-08-06 21:30:00 +0800
categories: [FFmpeg,音频,视频]
location: 西安
excerpt_separator: "```"
---

#### 一、先安装FFmpeg
1. 下载 FFmpeg：
   访问https://github.com/BtbN/FFmpeg-Builds/releases，下载ffmpeg-master-latest-win64-gpl-shared.zip
2. 安装FFmpeg：
   解压 ZIP 文件到`D:\ffmpeg-master-latest-win64-gpl-shared`（或其他目录）。
   添加 FFmpeg 到 PATH：按 `Win + R`，输入 `sysdm.cpl` 打开 系统属性。
   进入 高级 → 环境变量。
   在 系统变量 中找到 `Path`，点击 编辑。
   点击 新建，输入`D:\ffmpeg-master-latest-win64-gpl-shared\ffmpeg-master-latest-win64-gpl-shared\bin`（替换为你的 FFmpeg 路径）。
   点击 确定 保存。
3. 验证是否安装OK
   执行`ffmpeg -version`后输出版本号

#### 二、原视频去除音频
`ffmpeg -i Audio_Ori.mp4 -c:v copy -an Audio_NoVoice.mp4`


#### 三、视频叠加音频
1. 合成.pcm后缀音频到视频:

`ffmpeg -i ../Audio_NoVoice.mp4 -f s16le -ar 16000 -ac 1 -i jubian.pcm -c:v copy -c:a aac Audio_jubian_Test.mp4`

2. 合成.wav后缀音频到视频:

`ffmpeg -i Audio_NoVoice.mp4 -i 警报器_1.wav -c:v copy -c:a aac -strict experimental Audio_baojin.mp4`

3. 截取音频

```
ffmpeg -i AudioTest.mp4 -ss 00:00:00 -t 00:00:05 -c copy AudioTest_new.mp4
```

