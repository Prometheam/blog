---
layout: post_layout
title: "代码优化之noexcept"
date: 2025-08-29 19:15:24 +0800
categories: [C++语言]
location: 西安
excerpt_separator: "```"
---
是 C++11 引入的关键字，用于指定函数是否会抛出异常。它的主要作用是:

1. **性能优化**：编译器可能为  函数生成更高效的代码（如省略异常处理逻辑）。

2. **移动语义**：标准库（如 ）在扩容时，若元素类型的移动操作是 ，会优先使用移动而非复制。

3. **契约声明**：明确告知调用者函数不会抛出异常，简化错误处理。

- **作为说明符（Specifier）**：声明函数不会抛出异常

</pre>  - **条件性 **（C++11 起）：

```

void bar() noexcept(true);  // 等价于 noexcept

void baz() noexcept(false); // 可能抛出异常

```

- **模板中的条件 **：

```

template&lt;typename T&gt;

void swap(T&amp; a, T&amp; b) noexcept(noexcept(a.swap(b))) {

a.swap(b); // 仅当 a.swap(b) 不抛异常时，整个函数才是 noexcept

}

```


- **作为运算符（Operator）**：检查表达式是否声明为 ，返回 。

```

bool is_noexcept = noexcept(func());  // 检查 func() 是否声明为 noexcept

```
