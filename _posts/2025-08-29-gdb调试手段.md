---
layout: post_layout
title: "gdb调试手段"
date: 2025-08-29 19:24:57 +0800
categories: [工具与效率]
location: 西安
excerpt_separator: "```"
---
### 程序死锁问题定位(Debug版本库加载)

1.获取进程ID


2.gdb调试


3.查看进程当前加载的库及其内存地址


输出：


记录下 libexample.so 的加载地址 **（后续替换 Debug 库时需要）。

**或者：**


需要的是 **代码段的加载基地址**（即  的起始地址）

全量输出示例


4.**强制加载 Debug 版本的符号** 覆盖现有符号


- 此方法 **不会替换内存中的代码**，只是让 GDB 使用 Debug 版本的符号信息（如函数名、行号、变量等）。

- 如果 Debug 和 Release 库的代码布局不同（如优化导致函数地址变化），调试时可能会看到不一致的行为。
